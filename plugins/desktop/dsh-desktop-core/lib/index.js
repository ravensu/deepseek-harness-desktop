/**
 * Host half — no remote services; UI talks to Electron via window.dshDesktop.
 */
export const name = 'dsh-desktop-core';
export const inject = [];

export function apply() {
  // intentionally empty
}
