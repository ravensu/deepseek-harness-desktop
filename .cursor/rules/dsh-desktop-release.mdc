---
description: 发版只上传代码，push main 后 CI 自动打 tag 并发 GitHub Release
alwaysApply: true
---

# 发版

用户说「发版 / 上传 / release」时：

1. 本地：测过 → 提交 → 推 `origin main`。不要本地打 `v*` tag。
2. push main 触发 Package workflow：四端打包成功后自动打下一个 `v*` patch tag（同一 commit 重跑幂等复用）。
3. tag 推送后由 Package 显式 dispatch Release workflow（`GITHUB_TOKEN` 推的 tag 不会触发 on:push，必须 dispatch）：四端正式打包 + 发布 GitHub Release（含 `latest*.yml`，electron-updater 会广播给所有旧壳）。
4. 注意：凡是命中 package.yml paths（src/**、scripts/**、plugins/** 等）的 push 都会发新版并推送给全部用户，文档类改动不会触发。
5. 失败处理：Package 红 → 修 workflow/脚本推 main 重跑；Release 红 → 修复推 main 后手动补打下一个 tag（不要 force push 已发布的 tag / main）。
6. 不要本机 `pnpm dist*` 当正式发版。

细节见 `.cursor/skills/dsh-desktop-release/SKILL.md`。
